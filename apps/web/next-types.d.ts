/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Tracked twin of the generated next-env.d.ts (gitignored), so `bun typecheck` works on a fresh
// clone before the first `next build`: static image imports such as `./icon.png` need these types.
